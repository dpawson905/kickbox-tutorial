extends layout

block content
  .hero
    h1#hero-text.text-center.text-white.display-1
  if (currentUser && isAuthenticated)
    .container 
      .row 
        .col-12
          .auth-text
            h1.text-center.text-secondary.display-3(data-aos="flip-up" data-aos-duration="2000") Check it out... You're logged in
  script.
    $(document).ready(function () {
      AOS.init();

      let app = document.getElementById('hero-text');
      let typewriter = new Typewriter(app, {
        loop: false
      });
      typewriter.typeString('kickboxTutorial()')
        .start();
    });
    